package at.cihan.games.owngame;

import org.newdawn.slick.GameContainer;
import org.newdawn.slick.Input;
import org.newdawn.slick.SlickException;

import java.awt.*;

/*
public class Snake implements Actor{
    private float x, y;
    private Image SnakeImage;

    public Snake() throws SlickException {
        Image tmp = new Image("testdata/images.jpg");
        this.headImage = tmp.getScaledCopy(50, 50);
        this.x = 100;
        this.y = 100;
    }

    @Override
    public void update(GameContainer gameContainer, int delta) {
        Input input = gameContainer.getInput();
        float speed = 0.2f * delta;

        if (input.isKeyDown(Input.KEY_RIGHT)) {
            this.x += speed;
        }
        if (input.isKeyDown(Input.KEY_LEFT)) {
            this.x -= speed;
        }
        if (input.isKeyDown(Input.KEY_UP)) {
            this.y -= speed;
        }
        if (input.isKeyDown(Input.KEY_DOWN)) {
            this.y += speed;
        }
    }

    public float getX() {
        return this.x;
    }

    public float getY() {
        return this.y;
    }

    public void setX(float x) {
        this.x = x;
    }

    public void setY(float y) {
        this.y = y;
    }

    }

    @Override
    public void render(Graphics graphics) {
        headImage.draw(this.x, this.y);
    }

 */


